# Copy this file to .env and replace the password before using Compose.
# These values are examples for local learning only, not production credentials.
POSTGRES_DB=fullstackdb
POSTGRES_USER=fullstack_user
POSTGRES_PASSWORD=replace_with_a_local_development_password
BACKEND_HOST_PORT=8080
FRONTEND_HOST_PORT=8081
