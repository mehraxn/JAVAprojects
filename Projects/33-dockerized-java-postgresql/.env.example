# Copy to .env for local experimentation, then replace the password.
# Never commit a real .env file.
POSTGRES_DB=taskdb
POSTGRES_USER=task_user
POSTGRES_PASSWORD=replace_with_a_local_learning_password
