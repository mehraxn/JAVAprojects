# Intentionally INVALID: "production" is not an allowed environment value
# (only dev, staging, prod). Terraform must reject this file.
project_name = "iac-starter"
environment  = "production"
components   = ["web"]
