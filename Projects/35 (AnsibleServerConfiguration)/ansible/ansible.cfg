[defaults]
# The real inventory.ini is intentionally ignored. Create it from the example
# only after an approved disposable lab host is available.
inventory = inventory.ini
roles_path = roles
host_key_checking = True
retry_files_enabled = False
interpreter_python = auto_silent

[privilege_escalation]
become = True
become_method = sudo
become_ask_pass = True
