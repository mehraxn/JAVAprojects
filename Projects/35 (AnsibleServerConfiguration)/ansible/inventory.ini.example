# Documentation-only inventory. example.invalid cannot resolve to a real host.
[app_servers]
app-lab-01 ansible_host=server.example.invalid ansible_user=lab_user ansible_ssh_private_key_file=~/.ssh/replace_with_lab_key

[app_servers:vars]
ansible_port=22
