{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "java-app.fullname" . }}
type: Opaque
stringData:
  EXAMPLE_SECRET: {{ .Values.secret.exampleValue | quote }}
{{- end }}

# Example only. Keep secret.create false until a secret strategy is approved.
