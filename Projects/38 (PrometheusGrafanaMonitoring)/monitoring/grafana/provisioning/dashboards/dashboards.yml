apiVersion: 1

providers:
  - name: Phase 3 starter dashboards
    type: file
    disableDeletion: true
    editable: false
    options:
      path: /var/lib/grafana/dashboards

# TODO: Confirm folder organization and update behavior after dashboards contain real panels.
