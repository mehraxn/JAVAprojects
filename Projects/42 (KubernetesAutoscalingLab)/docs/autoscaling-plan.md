# Autoscaling Plan

Measure a stable single-Pod baseline before changing replica limits or targets. HPA CPU utilization depends on container CPU requests. VPA is recommendation-only in this starter to avoid automatic Pod changes.

TODO: define workload shape, metrics source, stabilization windows, success criteria, cost/capacity limits, and cleanup.
