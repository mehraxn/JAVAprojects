name: secure-pipeline-example

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  design-only:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      # TODO: Add approved compile, test, dependency, secret, SAST, SBOM,
      # image, signing, policy, and gated deployment stages.
