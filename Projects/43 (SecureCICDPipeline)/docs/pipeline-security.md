# Pipeline Security

Planned controls include minimal token permissions, pinned dependencies, isolated builds, masked secrets, immutable artifacts, provenance, policy checks, and explicit environment approval.

TODO: document the selected CI provider, trust boundaries, artifact flow, failure policy, and incident response.
