# Threat Model

Consider malicious pull requests, dependency compromise, credential exposure, untrusted build output, mutable tags, runner persistence, artifact substitution, and unauthorized deployment.

TODO: record assets, actors, entry points, mitigations, residual risks, and owners.
