services:
  app:
    image: my-observable-java-app:latest
  prometheus:
    image: prom/prometheus:example
  grafana:
    image: grafana/grafana:example
  loki:
    image: grafana/loki:example
  otel-collector:
    image: otel/opentelemetry-collector:example

# TODO: Pin real reviewed versions and add local networks, volumes, configs,
# health checks, and placeholder-only credentials before any future use.
