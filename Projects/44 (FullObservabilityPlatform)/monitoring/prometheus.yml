global:
  scrape_interval: 15s

scrape_configs:
  - job_name: java-app
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:8080

# TODO: Add rule files after the application metric contract is implemented.
