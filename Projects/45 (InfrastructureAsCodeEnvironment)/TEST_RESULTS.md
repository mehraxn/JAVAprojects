# Test Results — Infrastructure as Code Environment

Records the outcome of the safe validation steps in [TESTING.md](TESTING.md).
**No real infrastructure was created; `terraform apply` was not run.**

- Date: 2026-07-09
- Environment: Windows 11, Python 3.14.6
- Tooling present: Python 3 (with PyYAML). **Terraform and Ansible are NOT
  installed on this machine**, so their steps were not executed here — see
  "Tools unavailable" below. Re-run those steps on a machine with the tools and
  paste the real output into the matching section.

## Terraform fmt

Command:

```
terraform fmt -check -recursive terraform
```

Result: **NOT RUN — Terraform not installed.** Paste real output here after
running the command on a machine with Terraform.

## Terraform validate — dev

Commands:

```
terraform -chdir=terraform/environments/dev init -backend=false
terraform -chdir=terraform/environments/dev validate
```

Result: **NOT RUN — Terraform not installed.** Paste real output here after
running the commands. Expected: `Success! The configuration is valid.`

## Terraform validate — prod

Commands:

```
terraform -chdir=terraform/environments/prod init -backend=false
terraform -chdir=terraform/environments/prod validate
```

Result: **NOT RUN — Terraform not installed.** Paste real output here after
running the commands. Expected: `Success! The configuration is valid.`

## Inventory generation

Command:

```
python3 scripts/generate-inventory.py \
  examples/terraform-output/ansible_hosts.dev.json \
  ansible/inventory.ini
```

Result: **PASS (really executed).** Output:

```
Wrote 1 host(s) to ansible/inventory.ini (group [app]).
```

Generated `ansible/inventory.ini`:

```
# Generated by scripts/generate-inventory.py — do not edit by hand.
# Regenerate from Terraform output; this file is git-ignored.

[app]
iac-lab-dev-app-0 ansible_host=10.10.1.10 ansible_user=ubuntu environment=dev

[app:vars]
app_environment=dev
```

The prod sample (`examples/terraform-output/ansible_hosts.prod.json`) and the
wrapped `terraform output -json` object form were also generated successfully.

## Ansible syntax-check

Command:

```
ansible-playbook -i ansible/inventory.ini ansible/playbook.yml --syntax-check
```

Result: **NOT RUN — Ansible not installed.** Paste real output here after
running the command on a machine with Ansible.

## YAML parse

All Ansible YAML files were parsed with Python + PyYAML.

Result: **PASS (really executed).** Output:

```
OK   ansible/playbook.yml
OK   ansible/group_vars/all.yml
OK   ansible/roles/app/defaults/main.yml
OK   ansible/roles/app/handlers/main.yml
OK   ansible/roles/app/tasks/main.yml
OK   ansible/roles/common/tasks/main.yml
```

The example JSON files (`examples/terraform-output/*.json`) also parse as valid
JSON (they are consumed successfully by the generator above).

## Secret scan / no real secrets

Scanned all `*.tf`, `*.yml`, `*.ini*`, `*.json`, `*.cfg` for credential-like
strings (password/secret/api key/private key/token/AWS keys).

Result: **PASS (really executed).** No hardcoded secrets found; the only matches
are documentation and safety notes (e.g. "use ansible-vault", "never store a
password here"). No `198.51.100.x` remains under a `private_ip` field; all
private IPs are RFC 1918 (`10.x`). Backend examples use literal
`REPLACE_WITH_REAL_*` placeholders.

## Tools unavailable

- **Terraform** — not installed on this machine. `fmt`, `init`, and `validate`
  were not executed here. The configuration was reviewed statically and is
  structured so `validate` succeeds (env roots declare variables with defaults
  and pass them to the shared module; no provider is required).
- **Ansible** — not installed on this machine. `--syntax-check` and `--check`
  were not executed here. YAML was validated with PyYAML instead.

## Known limitations

- No `terraform apply` and no real cloud resources — every resource is a built-in
  `terraform_data` record by design.
- `--check` mode needs reachable hosts; the placeholder RFC 1918 IPs are not
  reachable, so only `--syntax-check` is meaningful offline.
- Terraform/Ansible results above must be filled in from a machine that has those
  tools; they are intentionally not faked here.
