# Golden Path

The golden path should reduce repeated setup without hiding ownership or blocking justified exceptions. Initial outputs should include a small Java app, tests, container definition, chart, pipeline template, documentation, and operational metadata.

TODO: define template inputs, generated files, acceptance criteria, upgrade strategy, and exception process.
