# Example only. Repository and cluster values are placeholders.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: generated-java-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://example.invalid/platform-generated-service.git
    targetRevision: main
    path: deployment
  destination:
    server: https://kubernetes.default.svc
    namespace: platform-learning
  # TODO: Define sync policy, ownership, and deletion protection.
