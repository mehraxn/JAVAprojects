# DEV overrides — small and chatty for fast iteration.
replicaCount: 1
config:
  environment: dev
  logLevel: DEBUG
resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 128Mi
