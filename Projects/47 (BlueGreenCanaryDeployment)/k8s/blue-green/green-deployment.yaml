# GREEN = new version v2, deployed beside blue before the Service switch. Example manifest; not deployed here.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-app-green
  labels:
    app: java-app
    track: green
    version: v2
spec:
  replicas: 3
  selector:
    matchLabels:
      app: java-app
      track: green
  template:
    metadata:
      labels:
        app: java-app
        track: green
        version: v2
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: app
          image: registry.example.invalid/blue-green-canary-app:v2        # placeholder image; replace with your built image
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: APP_VERSION
              value: v2
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
