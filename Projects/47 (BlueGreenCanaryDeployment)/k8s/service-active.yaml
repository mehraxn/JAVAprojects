apiVersion: v1
kind: Service
metadata:
  name: java-app-active
spec:
  selector:
    app: java-app
    track: blue
  ports:
    - name: http
      port: 80
      targetPort: 8080

# TODO: Switch `track` only after preview validation and explicit approval.
