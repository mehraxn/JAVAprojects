# Environment Strategy

The Kubernetes base remains common while overlays provide bounded non-sensitive differences. The same immutable artifact should move forward; it should not be rebuilt per environment.

TODO: define accounts/clusters, configuration, secrets, data, access, observability, cost, and cleanup boundaries.
