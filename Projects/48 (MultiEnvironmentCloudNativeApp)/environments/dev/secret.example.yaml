# EXAMPLE ONLY — never commit a real Secret. Copy to secret.yaml (git-ignored)
# and fill from a secrets manager / sealed-secret, OR let External Secrets create
# it. The values below are obvious placeholders, NOT real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: app-dev
type: Opaque
stringData:
  # Placeholder values. Do NOT put real secrets in Git.
  DB_PASSWORD: "REPLACE_ME_dev_placeholder"
  API_TOKEN: "REPLACE_ME_dev_placeholder"
