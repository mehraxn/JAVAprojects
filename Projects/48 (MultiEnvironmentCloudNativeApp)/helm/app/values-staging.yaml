replicaCount: 2
image:
  digest: "sha256:REPLACE_WITH_STAGING_DIGEST"
config:
  environment: staging
  logLevel: INFO
  featureNewUi: "true"
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi
