# EXAMPLE ONLY — never commit a real Secret. Placeholders, not real credentials.
# In prod especially, secrets should come from a sealed-secret or an External
# Secrets Operator backed by a real secrets manager — never plaintext in Git.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: app-prod
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_ME_prod_placeholder"
  API_TOKEN: "REPLACE_ME_prod_placeholder"
