# EXAMPLE ONLY — never commit a real Secret. Placeholders, not real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: app-staging
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_ME_staging_placeholder"
  API_TOKEN: "REPLACE_ME_staging_placeholder"
