# Recovery Flow

```text
failure detection -> incident decision -> isolate target -> select backup
-> restore -> integrity checks -> application checks -> controlled traffic
-> evidence and retrospective
```

Conceptual only. No recovery procedure was performed.
