# Suspended example only. It cannot perform a real backup.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: postgres-backup-example
spec:
  schedule: "0 2 * * *"
  suspend: true
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: backup
              image: postgres:placeholder
              command: ["/bin/sh", "-c"]
              args:
                - "echo 'TODO: configure an approved backup destination' && exit 1"
              # TODO: Add secrets through an approved mechanism, never this file.
